# Pinned for reproducible model output — the data-refresh pipeline (fetch_elo +
# fetch_api_results + compute_momentum). Known-compatible set on Python 3.12.
# To lock to EXACTLY what CI installed, run `pip freeze` after the first green
# workflow run and copy the versions here.
numpy==2.1.3
pandas==2.2.3
scipy==1.14.1
scikit-learn==1.5.2
xgboost==2.1.3
requests==2.32.3
